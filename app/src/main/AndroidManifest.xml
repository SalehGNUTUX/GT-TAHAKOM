<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- صلاحيات الشبكة لوسائل النقل عبر WiFi (Android TV / Roku / Samsung / LG / Sony / Broadlink) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- مطلوبة لاكتشاف الأجهزة عبر mDNS/SSDP في المرحلة 1 -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <!-- الأشعة تحت الحمراء: الباعث اختياري (بخلاف IRRemote) كي يعمل التطبيق على كل الهواتف -->
    <uses-permission android:name="android.permission.TRANSMIT_IR" />
    <uses-feature
        android:name="android.hardware.consumerir"
        android:required="false" />

    <application
        android:name=".TahakomApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.Tahakom">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Tahakom">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            استيراد حزمة ريموت مشتركة (.tahakom).
            عند نقر المستخدم على ملف مشترَك يفتح هنا مباشرة في تطبيقنا.
            يلتقط ثلاث حالات: فتح ملف، رابط عميق tahakom://import، ومشاركة نص.
        -->
        <activity
            android:name=".feature.share.ImportActivity"
            android:exported="true"
            android:label="@string/import_title"
            android:theme="@style/Theme.Tahakom">

            <!-- 1) فتح ملف بامتداد .tahakom أو نوع MIME المخصّص -->
            <intent-filter android:label="@string/import_title">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="application/vnd.gnutux.tahakom+json" />
            </intent-filter>

            <!-- مطابقة الامتداد عند غياب نوع MIME (مزوّدات لا تعرف النوع المخصّص) -->
            <intent-filter android:label="@string/import_title">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:host="*" />
                <data android:mimeType="*/*" />
                <data android:pathPattern=".*\\.tahakom" />
                <data android:pathPattern=".*\\..*\\.tahakom" />
            </intent-filter>

            <!-- 2) رابط عميق: tahakom://import?... -->
            <intent-filter android:label="@string/import_title">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="tahakom" android:host="import" />
            </intent-filter>

            <!-- 3) استقبال مشاركة من تطبيق آخر -->
            <intent-filter android:label="@string/import_title">
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/vnd.gnutux.tahakom+json" />
            </intent-filter>
        </activity>

        <!-- مشاركة ملفات .tahakom المُصدَّرة بأمان عبر URI مؤقت -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
